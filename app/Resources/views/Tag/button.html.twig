<{{ type | default('a') }} {% if(url | default(null)) %} href="{{ url }}" {% endif %} title="{{ tag.name }}" style="border: 0; {% if(tag.color) %}color: white; border-color: {{ tag.color }}; background: {{ tag.color }}{% endif %}" class="btn btn-{{ size | default('sm') }} btn-default">{% if(tag.icon) %}<span class="glyphicon glyphicon-{{ tag.icon }}"></span> {% endif %}{{ tag.name }} {% if(badge | default(false)) %}&nbsp;<span class="badge" style="font-size: 11px; font-weight: bold; {% if(tag.color) %}color: {{ tag.color }}; background: #fff;{% endif %}">{{ badge }}</span> {% endif %} </{{ type | default('a') }}>