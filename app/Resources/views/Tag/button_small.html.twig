<a href="{{ url }}" data-toggle="tooltip" data-placement="auto" title="{{ tag.name }}" data-container="body" title="{{ tag.name }}" style="padding-left: 2px; padding-right: 2px;width: 32px;border: none; background: transparent;{% if(tag.color) %}color: white; border: none; background: {{ tag.color }}{% endif %}" class="btn btn-sm btn-default">{% if(tag.icon) %}<span class="glyphicon glyphicon-{{ tag.icon }}"></span> {% endif %}{% if(not tag.icon) %}{{ tag.name | slice(0, 3) | upper }}</small>{% endif %}</a>