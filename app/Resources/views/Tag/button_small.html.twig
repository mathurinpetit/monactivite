<button title="{{ tag.name }}" style="padding-left: 2px; padding-right: 2px;width: 32px;border-color: #fff;{% if(tag.color) %}color: white; border-color: {{ tag.color }}; background: {{ tag.color }}{% endif %}" class="btn btn-sm btn-default">{% if(tag.icon) %}<span class="glyphicon glyphicon-{{ tag.icon }}"></span> {% endif %}{% if(not tag.icon) %}{{ tag.name | slice(0, 2) | upper }}</small>{% endif %}</button>